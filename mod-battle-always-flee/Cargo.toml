[package]
name = "mod-battle-always-flee"
edition = "2021"
version.workspace = true

[lib]
crate-type = ["cdylib"]
name = "battle_always_flee"

[dependencies]
log = { workspace = true }
win_dbg_logger = { workspace = true }
hooklet = { workspace = true }
p3-api = { path = "../p3-api" }
